# `@rom-weaver/win32-arm64-msvc`

Native Windows arm64 binary for [`rom-weaver`](https://www.npmjs.com/package/@rom-weaver/cli).

This package is installed automatically by `@rom-weaver/cli` on Windows on ARM. Most users should install the CLI package instead.
